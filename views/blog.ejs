<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cards Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto py-10">
        <h1 class="text-3xl text-center font-bold mb-8">My Cards</h1>

        <div class="flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-lg px-4 py-2 text-white hover:bg-red-600">Logout</a>
        </div>

         <!-- Create Button -->
         <div class="text-start mb-8">
            <a href="/blog/create" class="bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600">Create New Blog</a>
        </div>


        <!-- Card container -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">

            <% blogs.forEach(function(blog){ %>
                <div class="bg-white p-4 rounded-lg shadow-lg">
                    <img src="data:image/jpeg;base64,<%= blog.image.toString('base64') %>" alt="Card Image" class="w-full h-32 object-cover rounded-md mb-4">
                    <h2 class="text-xl font-semibold mb-2"><%= blog.title %></h2>
                    <p class="text-gray-600 mb-4"><%= blog.description %></p>
                    <div class="flex justify-between">
                        <a class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600" href="/edit/<%= blog._id %>">Edit</a>
                        <a class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600" href="/delete/<%= blog._id %>">Delete</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>


